{
  "compilerOptions": {
    "jsx": "preserve",
    "jsxImportSource": "solid-js",
    "moduleResolution": "node",
    "module": "esnext",
    "target": "esnext",
    // other options...
  },
  // Include these paths if not already present to ensure proper resolution
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist", "build"]
}